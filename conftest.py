# This repository is the contents of custom_components/myleonardo, so the root
# __init__.py is the Home Assistant integration initializer, not a pytest package
# initializer. Ignore it for lightweight local tests.
collect_ignore = ["__init__.py"]
